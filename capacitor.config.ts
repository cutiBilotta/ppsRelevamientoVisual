import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ppsRelevamientoVisual.app',
  appName: 'ppsRelevamientoVisual',
  webDir: 'www'
};

export default config;
